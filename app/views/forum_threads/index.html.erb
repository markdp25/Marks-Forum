<h1>All Forums</h1>

<div class="row">
  <div class="col-md-8">
    <% @forum_threads.each do |forum| %>
      <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
              <%= link_to @forum_thread.title, forum_thread %>
            </h4>
              <h4 class="media-heading">
                <%= link_to @forum_thread.title, forum_thread %>
              </h4>
        </div>
      </div>
    <% end %>
  </div>

  <div class="col-md-4">
    <%= link_to "New Forum", new_forum_thread_path, class: 'btn btn-success' %>
  </div>
</div>
